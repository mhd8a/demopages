---
---
[   
    {% assign pages = site.pages | where: 'dir', '/_pages' %}
    {% for page in pages %} 
       {
        "title"    : "{{ page.title | escape }}",
        "description" : "{{ page.description | strip_html | strip_newlines | escape }}",
        "url" : "{{ site.baseurl }}{{ page.url }}"
       } {% unless forloop.last %},{% endunless %} 
    {% endfor %}
]
